---
description: Automatically stage and commit all changes in the current repository
---

// turbo-all

1. Stage all changes
```powershell
git add .
```

2. Commit changes with a descriptive message
```powershell
git commit -m "chore: automatic commit of repository changes"
```
