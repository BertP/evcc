network:
  schema: http
  host: evcc.local # .local suffix announces the hostname via mDNS
  port: 7070

interval: 10s # control cycle interval

# log: debug

# To configure evcc, run:
# docker run -v $(pwd)/evcc.yaml:/etc/evcc.yaml -it evcc/evcc:latest evcc configure
